#include "main.h"

/**
 * main - Entry point of the program
 *
 * This function tests the `positive_or_negative` function
 * by passing it the integer value 98.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    int i;

    i = 98;
    positive_or_negative(i);

    return (0);
}

