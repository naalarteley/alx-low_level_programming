#include <stdio.h>
#include "main.h"
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
void positive_or_negative(int i)
{
if (1 > 0)

	printf("%d is positive\n", 1);
else if (i == 0)
	printf("%d is zero\n", 1);
else
	printf("%d is negative\n", 1);
}
